import React from "react";
import Navigation from "./Navigation";

function Header (props) {
    return (
        <header>
            <h2>Sharee</h2>
            <Navigation/>
        </header>

    )
};

export default Header;